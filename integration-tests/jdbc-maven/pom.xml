<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.bloomberg.selekt.samples</groupId>
    <artifactId>selekt-jdbc-maven-consumer</artifactId>
    <version>1.0-SNAPSHOT</version>

    <properties>
        <consumer.java.version>25</consumer.java.version>
        <maven.compiler.release>${consumer.java.version}</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <selekt.expected.backend>JNI</selekt.expected.backend>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.bloomberg.selekt</groupId>
                <artifactId>selekt-bom</artifactId>
                <version>${selekt.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>com.bloomberg.selekt</groupId>
            <artifactId>selekt-jdbc</artifactId>
        </dependency>
    </dependencies>

    <build>
        <directory>${project.basedir}/target/java${consumer.java.version}</directory>
        <sourceDirectory>${project.basedir}/../jdbc-smoke/src/main/java</sourceDirectory>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.15.0</version>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.6.2</version>
                <executions>
                    <execution>
                        <id>jdbc-smoke-test</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>java</goal>
                        </goals>
                        <configuration>
                            <mainClass>com.bloomberg.selekt.samples.JdbcSmokeTest</mainClass>
                            <arguments>
                                <argument>${selekt.expected.backend}</argument>
                            </arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
